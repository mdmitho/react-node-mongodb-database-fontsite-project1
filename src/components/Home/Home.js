import React from 'react';
import { Link } from 'react-router-dom';

const Home = () => {
    return (
        <div>
            <Link to='/user/add'>user</Link>
        </div>
    );
};

export default Home;